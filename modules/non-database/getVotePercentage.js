/*  getVotePercentage - Fungsi yang digunakan untuk menghitung persentase suara
    yang diberikan oleh user 

    Fungsi akan diberikan sebuah array yang berisi data user.
    Di dalam data user, terdapat key `vote` yang berisi ID paslon yang dipilih oleh user.
    Value dari key `vote` akan bernilai `null` jika user belum melakukan voting.
    Skema data user dapat dilihat pada file /documentation/server-user-data.md

    Fungsi mengembalikan sebuah object dengan key berupa id paslon dan value berupa
    persentase suara yang diraih paslon tersebut.

    Beberapa hal yang bisa dicari untuk penyelesaian:
    - Looping sebuah array
    - Mengubah nilai dari sebuah variabel
    - Pembuatan object
*/

// Abaikan bagian yang berisi @param dan @returns
/**
 * @param {import("../../models/UserData").UserBase[]} data 
 * @returns {Record<string, number>}
 */
export function getVotePercentage(data) {
    
}
